server:
  port: 9000  # 9000번 포트로 실행

spring:
  application:
    name: demo

  datasource:
    platform: mysql
      url: jdbc:mysql://rising-db.czs2ohot9ryv.ap-northeast-2.rds.amazonaws.com/carrot
      # url 형식은 jdbc:mysql://RDS의 EndPoint/내부 DB 스키마명 입니다.
      username: admin # 만약 따로 설정하지 않으셨다면 기본값은 admin 입니다
      password: dbwo7827 # 비밀번호를 입력해주세요!
